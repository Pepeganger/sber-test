import React from 'react'
import { NestedList } from '../NestedList/index.tsx';

export const App: React.FC = () => {
    return (
        <React.StrictMode>
            <NestedList />
        </React.StrictMode>
    );
};
